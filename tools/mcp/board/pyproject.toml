[project]
name = "legion-mcp-board"
version = "0.1.0"
description = "MCP server: read-only view of the LEGION ticket board (docs/board.json and .scratch/*/issues/)."
requires-python = ">=3.11"
dependencies = ["mcp>=1.2,<2"]

[tool.uv]
package = false
