/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * @description `POST /api/auth/login`. Rate-limited 5/min per IP so a leaked email
         *     cannot be brute-forced into a working password - the household is two
         *     people, not a userbase, so five attempts a minute is generous, not
         *     tight.
         */
        post: operations["api_auth_login_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * @description `POST /api/auth/logout`. Revokes only the token making the request -
         *     every other device this user owns keeps working.
         *
         *     Send it with an explicit `Content-Length: 0` even though there is no
         *     body: measured against the deployed Cloud Run service on 2026-09-07, a
         *     POST carrying neither a body nor that header is answered 411 Length
         *     Required by the frontend and never reaches Django, so the token is NOT
         *     revoked and the caller has been told nothing about why. Most HTTP
         *     clients set the header themselves for an empty POST; `curl` without
         *     `-d` does not.
         */
        post: operations["api_auth_logout_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/auth/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET /api/auth/me`. This is the phone's membership check - if this
         *     call succeeds, the calling token is live and its user is a household
         *     member; if it 401s or 403s, the phone knows to ask for a new token.
         */
        get: operations["api_auth_me_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/bodyweight_logs/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_bodyweight_logs_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/bodyweight_logs/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_bodyweight_logs_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_bodyweight_logs_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/meal_logs/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_meal_logs_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/meal_logs/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_meal_logs_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_meal_logs_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/meal_targets/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_meal_targets_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/meal_targets/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_meal_targets_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_meal_targets_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/sleep_logs/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_sleep_logs_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/sleep_logs/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_sleep_logs_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_sleep_logs_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/sleep_targets/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_sleep_targets_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/sleep_targets/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_sleep_targets_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_sleep_targets_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/workout_plan_items/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_workout_plan_items_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/workout_plan_items/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_workout_plan_items_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_workout_plan_items_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/workout_plans/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_workout_plans_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/workout_plans/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_workout_plans_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_workout_plans_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/workout_set_logs/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_body_workout_set_logs_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/body/workout_set_logs/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_body_workout_set_logs_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_body_workout_set_logs_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/changes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get: operations["api_changes_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/checklists/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description `GET /api/checklists?since=<iso>` and `POST /api/checklists`. */
        get: operations["api_checklists_list"];
        put?: never;
        /** @description `GET /api/checklists?since=<iso>` and `POST /api/checklists`. */
        post: operations["api_checklists_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/checklists/{checklist_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description `GET`/`PATCH`/`DELETE /api/checklists/<checklist_id>`. */
        get: operations["api_checklists_retrieve"];
        put?: never;
        post?: never;
        /** @description `GET`/`PATCH`/`DELETE /api/checklists/<checklist_id>`. */
        delete: operations["api_checklists_destroy"];
        options?: never;
        head?: never;
        /** @description `GET`/`PATCH`/`DELETE /api/checklists/<checklist_id>`. */
        patch: operations["api_checklists_partial_update"];
        trace?: never;
    };
    "/api/checklists/{checklist_id}/items": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description `GET`/`POST /api/checklists/<checklist_id>/items`. */
        get: operations["api_checklists_items_list"];
        put?: never;
        /** @description `GET`/`POST /api/checklists/<checklist_id>/items`. */
        post: operations["api_checklists_items_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/checklists/{checklist_id}/items/{item_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description `GET`/`PATCH`/`DELETE /api/checklists/<checklist_id>/items/<item_id>`. */
        get: operations["api_checklists_items_retrieve"];
        put?: never;
        post?: never;
        /** @description `GET`/`PATCH`/`DELETE /api/checklists/<checklist_id>/items/<item_id>`. */
        delete: operations["api_checklists_items_destroy"];
        options?: never;
        head?: never;
        /** @description `GET`/`PATCH`/`DELETE /api/checklists/<checklist_id>/items/<item_id>`. */
        patch: operations["api_checklists_items_partial_update"];
        trace?: never;
    };
    "/api/checklists/{checklist_id}/items/{item_id}/tick": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * @description `POST /api/checklists/<checklist_id>/items/<item_id>/tick` -
         *     `{day, value?, source?}`. See `checklists/serializers.py`'s
         *     `TickRequestSerializer` for the measured-item refusal, reused verbatim
         *     from `ChecklistController.tick`.
         */
        post: operations["api_checklists_items_tick_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/checklists/{checklist_id}/items/{item_id}/tick/{day}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * @description `DELETE /api/checklists/<checklist_id>/items/<item_id>/tick/<day>` -
         *     soft-deletes the tick for that day, matching `ChecklistController.untick`.
         *     Idempotent: no tick on that day is still a 204, not a 404 - "already
         *     untouched" and "just unticked" read the same to a caller that does not
         *     care which happened.
         */
        delete: operations["api_checklists_items_tick_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description `GET /api/events?since=<iso>` and `POST /api/events`. */
        get: operations["api_events_list"];
        put?: never;
        /** @description `GET /api/events?since=<iso>` and `POST /api/events`. */
        post: operations["api_events_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/events/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** @description `PATCH`/`DELETE /api/events/<id>`. */
        delete: operations["api_events_destroy"];
        options?: never;
        head?: never;
        /** @description `PATCH`/`DELETE /api/events/<id>`. */
        patch: operations["api_events_partial_update"];
        trace?: never;
    };
    "/api/fleet/build_entries/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_build_entries_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/build_entries/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_build_entries_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_build_entries_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/chassis_quirks/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_chassis_quirks_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/chassis_quirks/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_chassis_quirks_update"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/code_clear_events/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_code_clear_events_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/code_clear_events/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_code_clear_events_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_code_clear_events_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/code_events/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_code_events_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/code_events/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_code_events_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_code_events_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/drive_reassignments/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_drive_reassignments_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/drive_reassignments/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_drive_reassignments_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_drive_reassignments_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/drives/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_drives_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/drives/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_drives_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_drives_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/maintenance_schedules/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_maintenance_schedules_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/maintenance_schedules/{vehicle_id}/{service_name}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT maintenance_schedules/<vehicle_id>/<service_name>/`.
         *
         *     Idempotent for the same reason the generic one is: the identity comes
         *     from the URL, so a retry cannot make a second row - and here the URL
         *     carries both halves of the unique constraint, so a retry cannot violate
         *     `maintenance_schedules_unique_per_vehicle` either.
         */
        put: operations["api_fleet_maintenance_schedules_update"];
        post?: never;
        /**
         * @description `DELETE maintenance_schedules/<vehicle_id>/<service_name>/`. Sets
         *     the tombstone; idempotent; a second delete is still a 204.
         */
        delete: operations["api_fleet_maintenance_schedules_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/obd_samples/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET /api/fleet/obd_samples/?vehicle=<uuid>&since=<iso>` - the windowed,
         *     per-vehicle pull `FleetBackend.fetchObdSamplesSince` calls.
         *
         *     No `?active=1`: this table has no `deleted_at`, so every row that exists is
         *     live and the unnarrowed feed already IS the active set. The parameter is
         *     absent from the schema rather than advertised and ignored, which is the
         *     same choice `api/synced.SyncedModelViewSet.has_tombstones` makes for the
         *     gated tables.
         *
         *     Paged with the same 500-row `next` cursor every other feed uses, keyed on
         *     `recorded_at`. The known limit `api/synced.py` states for its own cursor
         *     applies here too and is MORE reachable: the cursor is an inclusive
         *     timestamp and cannot express "after this row", so a full page of samples
         *     sharing one `recorded_at` would re-serve itself forever. It needs 500
         *     samples at one instant for one car and one PID set; the natural key makes
         *     exact duplicates impossible, so the 500 would have to be 500 distinct PIDs
         *     read in the same millisecond.
         */
        get: operations["api_fleet_obd_samples_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/obd_samples/batch/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * @description `POST /api/fleet/obd_samples/batch/` - `FleetBackend.uploadObdSampleBatch`.
         *
         *     The body is a JSON ARRAY of samples, matching ticket 04's own exceptions
         *     table (`POST /api/fleet/obd_samples/batch/ [..]`) and what
         *     `SupabaseFleetBackend` already sends.
         *
         *     **Idempotent on the natural key, by construction rather than by checking.**
         *     `obd_samples_natural_key_idx` is unique on `(vehicle_id, pid, recorded_at)`
         *     and the insert names it as the `on conflict` target, so a re-post of a
         *     batch that already landed writes nothing and answers 200 with
         *     `inserted: 0`. That is not a nicety: `ObdSampleReconcile` resumes an
         *     interrupted upload from a cursor it may have failed to advance, so re-posts
         *     are the normal case and not the exceptional one. A sample IS its car, its
         *     PID and its instant - the key is content-derived, so a re-post is genuinely
         *     the same sample and not merely one that looks like it.
         *
         *     **Nothing is corrected here and nothing can be.** There is no PUT, no
         *     PATCH and no DELETE anywhere on this table. A telemetry observation is
         *     superseded by a newer observation, never by editing an old one, which is
         *     why `obd_samples` carries no `forbid_mutation_of_facts` trigger either -
         *     the immutability trigger exists for gated aspects where a row asserts a
         *     VERIFIED fact, and this row asserts an observation.
         */
        post: operations["api_fleet_obd_samples_batch_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/obd_samples/count/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET /api/fleet/obd_samples/count/` - `FleetBackend.countObdSamples`.
         *
         *     A number and nothing else. `ObdSampleReconcile`'s own class doc explains at
         *     length why a full-row fetch here "would defeat the entire batching effort
         *     for a report line"; against PostgREST that was a `HEAD` with
         *     `Prefer: count=exact`, and here it is a `COUNT(*)`, which is the same
         *     bargain - one integer, no rows.
         *
         *     `?vehicle=` is OPTIONAL here, unlike on the feed, and the asymmetry is not
         *     an inconsistency: a count of the whole table is one aggregate whatever its
         *     size, so the reason the feed refuses an unbounded request simply does not
         *     apply.
         */
        get: operations["api_fleet_obd_samples_count"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/oil_analyses/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_oil_analyses_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/oil_analyses/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_oil_analyses_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_oil_analyses_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/service_history/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_service_history_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/service_history/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_service_history_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_service_history_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/vehicle_specs/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_vehicle_specs_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/vehicle_specs/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_vehicle_specs_update"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/vehicles/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_fleet_vehicles_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/fleet/vehicles/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_fleet_vehicles_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_fleet_vehicles_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ingest/files/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_ingest_files_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ingest/files/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET <table>/<identity>/`. One row, tombstoned or not.
         *
         *     Routed ONLY on a `GatedReadViewSet`, and it exists there because the
         *     405 has to have somewhere to happen. A PUT to a path no URL pattern
         *     matches is a 404, not a 405, and DRF refuses `as_view({})` outright
         *     ("The `actions` argument must be provided"), so a detail route that
         *     carried no action at all could not be declared. The minimum honest
         *     action on a read-only resource is the read, and it is keyed on the
         *     server's own `id` because none of the five gated tables has a
         *     client-minted identity that is reliably present: `statements` has no
         *     `origin_guid` column at all, and it is nullable on
         *     `ledger_transactions`, `receipts` and `receipt_line_items` (null on
         *     everything the gate itself committed - see
         *     `20260826000100_origin_guid.sql`).
         */
        get: operations["api_ingest_files_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ingest/receipt": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * @description `public.commit_receipt(payload jsonb)`, moved into Django.
         *
         *     The request body IS that RPC's `payload`, unchanged, and is a free-form JSON object on purpose (see this module's source for why it is not a serializer). Keys read: `content_sha256` (required), `provenance` (defaults to `LLM_RECONCILED`), `items` (array of `{name, quantity?, unit_price_cents?, total_price_cents, estimated_calories_kcal?, estimated_protein_g?, estimated_carbs_g?, estimated_fat_g?}`), the anchors `total_cents` / `subtotal_cents?` / `tax_cents?` / `other_charges_cents?`, `store`, `purchase_date`, `currency`, `photo_object_path?`, and the file facts `source_file_id?` / `display_name?` / `size_bytes?`. Money is an integer number of cents. **The four `estimated_*` fields are estimates and are excluded from the reconciliation arithmetic** (CLAUDE.md section 4 rule 5).
         */
        post: operations["api_ingest_receipt_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ingest/statement": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * @description `public.commit_statement(payload jsonb)`, moved into Django.
         *
         *     The request body IS that RPC's `payload`, unchanged, and is a free-form JSON object on purpose (see this module's source for why it is not a serializer). Keys read: `content_sha256` (required), `provenance` (required, and never `UNRECONCILED` - there is no provisional path here), `lines` (array of `{txn_date, description, amount_cents, balance_cents?, line_ref?, category?}`), the three anchors `stated_total_cents?` / `opening_balance_cents` / `closing_balance_cents`, `account_last4`, `account_nickname`, `currency`, `period_start?`, `period_end?`, and the file facts `source_file_id?` / `display_name?` / `size_bytes?`. Money is an integer number of cents, never a decimal.
         */
        post: operations["api_ingest_statement_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/budget_targets/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_ledger_budget_targets_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/budget_targets/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_ledger_budget_targets_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_ledger_budget_targets_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/categories/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_ledger_categories_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/categories/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_ledger_categories_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_ledger_categories_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/category_rules/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_ledger_category_rules_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/category_rules/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_ledger_category_rules_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_ledger_category_rules_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/statements/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_ledger_statements_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/statements/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET <table>/<identity>/`. One row, tombstoned or not.
         *
         *     Routed ONLY on a `GatedReadViewSet`, and it exists there because the
         *     405 has to have somewhere to happen. A PUT to a path no URL pattern
         *     matches is a 404, not a 405, and DRF refuses `as_view({})` outright
         *     ("The `actions` argument must be provided"), so a detail route that
         *     carried no action at all could not be declared. The minimum honest
         *     action on a read-only resource is the read, and it is keyed on the
         *     server's own `id` because none of the five gated tables has a
         *     client-minted identity that is reliably present: `statements` has no
         *     `origin_guid` column at all, and it is nullable on
         *     `ledger_transactions`, `receipts` and `receipt_line_items` (null on
         *     everything the gate itself committed - see
         *     `20260826000100_origin_guid.sql`).
         */
        get: operations["api_ledger_statements_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/transactions/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_ledger_transactions_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/ledger/transactions/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET <table>/<identity>/`. One row, tombstoned or not.
         *
         *     Routed ONLY on a `GatedReadViewSet`, and it exists there because the
         *     405 has to have somewhere to happen. A PUT to a path no URL pattern
         *     matches is a 404, not a 405, and DRF refuses `as_view({})` outright
         *     ("The `actions` argument must be provided"), so a detail route that
         *     carried no action at all could not be declared. The minimum honest
         *     action on a read-only resource is the read, and it is keyed on the
         *     server's own `id` because none of the five gated tables has a
         *     client-minted identity that is reliably present: `statements` has no
         *     `origin_guid` column at all, and it is nullable on
         *     `ledger_transactions`, `receipts` and `receipt_line_items` (null on
         *     everything the gate itself committed - see
         *     `20260826000100_origin_guid.sql`).
         */
        get: operations["api_ledger_transactions_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/memory/companion_memories/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included), `?active=1` (live rows
         *     only) and `?vehicle=<id>` (the recall rule: `car_anchored` memories
         *     only for that vehicle, `driver` and `relationship` always). All
         *     three compose.
         *
         *     A missing `since` means EVERYTHING, and so does a missing
         *     `vehicle` - this route is the replication feed as well as the recall
         *     read, and a replica has to be whole. See the `vehicle` parameter's
         *     own description.
         */
        get: operations["api_memory_companion_memories_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/memory/companion_memories/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_memory_companion_memories_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_memory_companion_memories_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/memory/memories/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_memory_memories_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/memory/memories/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_memory_memories_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_memory_memories_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/memory/memory_audit/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_memory_memory_audit_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/memory/memory_audit/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_memory_memory_audit_update"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/pantry/grocery_staples/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_pantry_grocery_staples_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/pantry/grocery_staples/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_pantry_grocery_staples_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_pantry_grocery_staples_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/pantry/line-items/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_pantry_line_items_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/pantry/line-items/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET <table>/<identity>/`. One row, tombstoned or not.
         *
         *     Routed ONLY on a `GatedReadViewSet`, and it exists there because the
         *     405 has to have somewhere to happen. A PUT to a path no URL pattern
         *     matches is a 404, not a 405, and DRF refuses `as_view({})` outright
         *     ("The `actions` argument must be provided"), so a detail route that
         *     carried no action at all could not be declared. The minimum honest
         *     action on a read-only resource is the read, and it is keyed on the
         *     server's own `id` because none of the five gated tables has a
         *     client-minted identity that is reliably present: `statements` has no
         *     `origin_guid` column at all, and it is nullable on
         *     `ledger_transactions`, `receipts` and `receipt_line_items` (null on
         *     everything the gate itself committed - see
         *     `20260826000100_origin_guid.sql`).
         */
        get: operations["api_pantry_line_items_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/pantry/receipts/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_pantry_receipts_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/pantry/receipts/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET <table>/<identity>/`. One row, tombstoned or not.
         *
         *     Routed ONLY on a `GatedReadViewSet`, and it exists there because the
         *     405 has to have somewhere to happen. A PUT to a path no URL pattern
         *     matches is a 404, not a 405, and DRF refuses `as_view({})` outright
         *     ("The `actions` argument must be provided"), so a detail route that
         *     carried no action at all could not be declared. The minimum honest
         *     action on a read-only resource is the read, and it is keyed on the
         *     server's own `id` because none of the five gated tables has a
         *     client-minted identity that is reliably present: `statements` has no
         *     `origin_guid` column at all, and it is nullable on
         *     `ledger_transactions`, `receipts` and `receipt_line_items` (null on
         *     everything the gate itself committed - see
         *     `20260826000100_origin_guid.sql`).
         */
        get: operations["api_pantry_receipts_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/places/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_places_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/places/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_places_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_places_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/voice_notes/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `?since=<iso>` (tombstones included) and `?active=1` (live rows
         *     only). They compose: `?active=1&since=<iso>` is a live-rows-changed
         *     feed, and `?active=1` alone is every live row since the epoch.
         *
         *     A missing `since` means EVERYTHING, never nothing - `api/sync.parse_since`
         *     holds that rule for the whole API and quotes the phone-side cursor
         *     comment it comes from.
         */
        get: operations["api_voice_notes_list"];
        put?: never;
        /**
         * @description `POST <table>/`. Routed only where the identity IS the primary
         *     key (voice notes) - everywhere else the client mints the identity
         *     and PUT is both create and update, so a second create path would be
         *     a second way to do one thing.
         */
        post: operations["api_voice_notes_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/voice_notes/{identity}/": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * @description `PUT <table>/<identity>/`. Idempotent by construction: the
         *     identity comes from the URL, so a retry cannot make a second row.
         *     200 whether the row was created or updated - the caller asked for
         *     the row to exist in this state and it does, and which of the two
         *     happened is not something a retrying client can act on.
         */
        put: operations["api_voice_notes_update"];
        post?: never;
        /**
         * @description `DELETE <table>/<identity>/`. Sets the tombstone column; the row
         *     stays, because a phone that has not synced since still needs to
         *     learn the row is gone. Idempotent - a second delete is still a 204,
         *     matching `EventsBackend.softDelete`'s own contract.
         */
        delete: operations["api_voice_notes_destroy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET /healthz`. Unauthenticated on purpose - a load balancer or an
         *     uptime check has no device token - and it says exactly what it checked:
         *     whether the database answered a query, nothing more. Section 7's
         *     outcome-verb rule applies here too: 'ok' is never returned unless the
         *     query actually ran.
         */
        get: operations["health_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * @description `GET /healthz`. Unauthenticated on purpose - a load balancer or an
         *     uptime check has no device token - and it says exactly what it checked:
         *     whether the database answered a query, nothing more. Section 7's
         *     outcome-verb rule applies here too: 'ok' is never returned unless the
         *     query actually ran.
         */
        get: operations["healthz_retrieve"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        BodyweightLog: {
            /** Format: uuid */
            readonly id: string;
            /** Format: double */
            weight_value: number;
            weight_unit: string;
            /** Format: date-time */
            logged_at: string;
            trust_tier: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Field-for-field `RemoteBudgetTarget` / `BudgetTargetFields`.
         *
         *     `amount_cents` is an integer. `effective_from_month` is a bare `date`, not
         *     a timestamp, matching `meal_targets.effective_from_date`'s precedent - the
         *     phone's `effectiveFromMonthEpoch` is always a UTC month-start instant by
         *     convention, so a date round-trips it with no timezone ambiguity.
         */
        BudgetTarget: {
            /** Format: uuid */
            readonly id: string;
            category: string;
            currency: string;
            /** Format: int64 */
            amount_cents: number;
            /** Format: date */
            effective_from_month: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Field-for-field `RemoteBuildEntry` / `BuildEntryUpload`: one
         *     driver-authored logbook line (a mod, part, repair, consumable, or general
         *     spend entry).
         */
        BuildEntry: {
            /** Format: uuid */
            readonly id: string;
            sync_id: string;
            /** Format: uuid */
            vehicle_id: string;
            /** @description 'mod', 'part', 'repair', 'consumable' or 'other' on the phone, and unconstrained text on the live schema - the list is open-ended, so this API refuses no value. */
            entry_type: string;
            title: string;
            /** @default  */
            vendor: string;
            /** @default  */
            part_number: string;
            /**
             * Format: int64
             * @description In cents (CLAUDE.md section 4 rule 3). Null means the driver logged what was done with no dollar figure; never 0, which would assert it was free.
             */
            cost_cents?: number | null;
            /** Format: date-time */
            logged_at: string;
            mileage?: number | null;
            /** @default  */
            notes: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        /** @description Field-for-field `RemoteCategory` / `CategoryFields`. */
        Category: {
            /** Format: uuid */
            readonly id: string;
            name: string;
            is_food_category: boolean;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Field-for-field `RemoteCategoryRule` / `CategoryRuleFields`.
         *
         *     `created_at_client` is writable and `created_at` is not, and the pair is
         *     deliberate rather than an oversight - the migration's own header explains
         *     it: `created_at` is Postgres's insert stamp, `created_at_client` is the
         *     instant the rule was written on the phone, and `LedgerController.applyCategoryRules`
         *     orders rules by the second one, oldest first, because the earliest rule
         *     written for a merchant is the one that governs it. Letting the server's
         *     clock stand in for the phone's would silently reorder every rule around
         *     whenever it happened to reach the server.
         */
        CategoryRule: {
            /** Format: uuid */
            readonly id: string;
            category: string;
            substring: string;
            /** Format: date-time */
            created_at_client: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        Changes: {
            /**
             * Format: date-time
             * @description Postgres's own clock, read before any row query. Store THIS as the next `since`, rather than a max(updated_at) computed from the rows returned.
             */
            server_time: string;
            events?: components["schemas"]["Event"][];
            checklists?: components["schemas"]["Checklist"][];
            checklist_items?: components["schemas"]["ChecklistItem"][];
            checklist_ticks?: components["schemas"]["ChecklistTick"][];
            places?: components["schemas"]["Place"][];
            voice_notes?: components["schemas"]["VoiceNote"][];
            bodyweight_logs?: components["schemas"]["BodyweightLog"][];
            meal_logs?: components["schemas"]["MealLog"][];
            meal_targets?: components["schemas"]["MealTarget"][];
            sleep_logs?: components["schemas"]["SleepLog"][];
            sleep_targets?: components["schemas"]["SleepTarget"][];
            workout_plans?: components["schemas"]["WorkoutPlan"][];
            workout_plan_items?: components["schemas"]["WorkoutPlanItem"][];
            workout_set_logs?: components["schemas"]["WorkoutSetLog"][];
            memories?: components["schemas"]["Memory"][];
            companion_memories?: components["schemas"]["CompanionMemory"][];
            memory_audit?: components["schemas"]["MemoryAudit"][];
            categories?: components["schemas"]["Category"][];
            category_rules?: components["schemas"]["CategoryRule"][];
            budget_targets?: components["schemas"]["BudgetTarget"][];
            statements?: components["schemas"]["Statement"][];
            ledger_transactions?: components["schemas"]["LedgerTransaction"][];
            grocery_staples?: components["schemas"]["GroceryStaple"][];
            receipts?: components["schemas"]["Receipt"][];
            receipt_line_items?: components["schemas"]["ReceiptLineItem"][];
            ingested_files?: components["schemas"]["IngestedFile"][];
            vehicles?: components["schemas"]["Vehicle"][];
            service_history?: components["schemas"]["ServiceHistory"][];
            drives?: components["schemas"]["Drive"][];
            code_events?: components["schemas"]["CodeEvent"][];
            code_clear_events?: components["schemas"]["CodeClearEvent"][];
            oil_analyses?: components["schemas"]["OilAnalysis"][];
            build_entries?: components["schemas"]["BuildEntry"][];
            drive_reassignments?: components["schemas"]["DriveReassignment"][];
            chassis_quirks?: components["schemas"]["ChassisQuirk"][];
            vehicle_specs?: components["schemas"]["VehicleSpec"][];
            maintenance_schedules?: components["schemas"]["MaintenanceSchedule"][];
        };
        /**
         * @description Field-for-field `RemoteChassisQuirk` / `ChassisQuirkUpload`.
         *
         *     `chassis` is a comma-delimited list of chassis codes, e.g. "E46,E46M3",
         *     carried verbatim rather than normalised into a join table - it matches the
         *     phone's own shape, and it is NOT a `vehicle_id`: this is platform reference
         *     data, independent of any specific vehicle row.
         *
         *     The cost and mileage bounds are null, never -1, when unknown. The phone
         *     stores -1 as its unbounded sentinel and that sentinel must not be carried
         *     forward - it is the zero-default trap wearing a different number.
         */
        ChassisQuirk: {
            quirk_id: string;
            chassis: string;
            /** @default  */
            engine: string;
            title: string;
            symptom: string;
            verification_steps: string;
            /** @description Null means no lower bound. Never -1, which is the phone's own sentinel and must not be carried forward. */
            mileage_low?: number | null;
            mileage_high?: number | null;
            severity: string;
            /**
             * Format: int64
             * @description Typical repair cost range, in cents (CLAUDE.md section 4 rule 3). Null means unknown or DIY-variable, never 0.
             */
            cost_low_cents?: number | null;
            /** Format: int64 */
            cost_high_cents?: number | null;
            /** @default  */
            fix_notes: string;
            /** @default  */
            source_url: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
        };
        Checklist: {
            /** Format: uuid */
            readonly id: string;
            name: string;
            schedule_kind?: string | null;
            schedule_every?: number | null;
            schedule_days_of_week?: string | null;
            sort_order?: number;
            archived?: boolean;
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            sync_id?: string | null;
        };
        ChecklistItem: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            checklist: string;
            text: string;
            sort_order?: number;
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            sync_id?: string | null;
            measure_unit?: string | null;
            /** Format: double */
            measure_target?: number | null;
            measure_direction?: string | null;
        };
        /**
         * @description Read/representation shape only - a tick is never created or edited
         *     through this serializer directly; `ChecklistItemTickView` builds
         *     `ChecklistTick` rows itself so it can apply the revive-on-retick
         *     semantics `ChecklistController.tick`/`ChecklistTickDao.retick` describe,
         *     which a plain `ModelSerializer.create()` cannot express (it does not
         *     know to look for a soft-deleted row occupying the same `(item, day)`
         *     slot first).
         */
        ChecklistTick: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            item: string;
            day: number;
            /** Format: date-time */
            readonly ticked_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            sync_id?: string | null;
            /** Format: double */
            value?: number | null;
            source?: string;
        };
        /**
         * @description Field-for-field `RemoteCodeClearEvent` / `CodeClearEventUpload`: the
         *     outcome of one Mode 04 clear-codes send.
         *
         *     A row exists only for CLEARED, RETURNED or UNVERIFIED. The two no-op
         *     outcomes (`NOTHING_TO_CLEAR`, `REFUSED`) never send anything and never
         *     reach this table, which is why they are not in the allowed set.
         */
        CodeClearEvent: {
            /** Format: uuid */
            readonly id: string;
            sync_id: string;
            /** Format: uuid */
            vehicle_id: string;
            /** Format: date-time */
            occurred_at: string;
            mileage?: number | null;
            codes_before: unknown;
            freeze_frame?: unknown;
            /** @description THREE-way, not two. Null means the post-send re-read never completed (UNVERIFIED). An empty array means it ran and found nothing (CLEARED). A non-empty array names the codes that survived (RETURNED). Do not collapse null and the empty array. */
            codes_after?: unknown;
            outcome: string;
            /**
             * @description The adapter's raw acknowledgement. DIAGNOSTIC ONLY: a failed send returns an empty string and a quiet link answers exactly like a real ack, so `outcome` is never asserted off this field.
             * @default
             */
            ack_raw: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        /**
         * @description Field-for-field `RemoteCodeEvent` / `CodeEventUpload`: one ELM327 DTC
         *     read plus its Mode 02 freeze frame.
         */
        CodeEvent: {
            /** Format: uuid */
            readonly id: string;
            sync_id: string;
            /** Format: uuid */
            vehicle_id: string;
            /** Format: date-time */
            occurred_at: string;
            /** @description An estimate, not a measurement: a frozen snapshot of the vehicle's current mileage at read time, with nothing captured to prove how stale it was. Diagnostic metadata, never a gated figure. */
            mileage?: number | null;
            /** @description A JSON array of DTC strings, e.g. ["P0420","P0128"]. */
            codes: unknown;
            /** @description A JSON object, or null when the adapter returned no freeze frame at all - some older ELM327 clones skip Mode 02 entirely. Null and an empty object are different facts. */
            freeze_frame?: unknown;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        /**
         * @description Field-for-field `RemoteCompanionMemory`. See this module's own doc
         *     comment for the embedding columns that exist on neither side.
         */
        CompanionMemory: {
            /** Format: uuid */
            readonly id: string;
            vehicle_id: string;
            text: string;
            category: string;
            source: string;
            /** @default 5 */
            importance: number;
            /** Format: date-time */
            logged_at: string;
            /** Format: date-time */
            last_accessed_at?: string | null;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description `{"detail": "..."}` - the shape every hand-written refusal in this API
         *     uses, and the shape DRF itself uses for 401/403/404/405. The strings are
         *     written to be shown to a person: they say what did NOT happen (CLAUDE.md
         *     section 7's outcome-verb rule), so a client should surface `detail`
         *     rather than inventing its own wording.
         */
        Detail: {
            detail: string;
        };
        /**
         * @description Field-for-field `RemoteDrive` / `DriveUpload`.
         *
         *     `gallons` is null, never 0.0, when MAF was silent for the whole drive.
         *     `Drive.gallons`' own comment is emphatic about this and the distinction is
         *     load-bearing for MPG: a drive that burned an unknown amount of fuel must
         *     not read as a drive that burned none.
         */
        Drive: {
            /** Format: uuid */
            readonly id: string;
            sync_id: string;
            /** Format: uuid */
            vehicle_id: string;
            /** Format: date-time */
            started_at: string;
            /** Format: date-time */
            ended_at: string;
            /** Format: double */
            miles: number;
            /**
             * Format: double
             * @description Null, never 0.0, when the mass-airflow sensor was silent for the whole drive. Unknown fuel and no fuel are different facts and must not collapse - an MPG figure computed over a null here is not a low MPG, it is no MPG.
             */
            gallons?: number | null;
            /** @description 'ENGINE_OFF' or 'LINK_LOST' today. Deliberately unconstrained text on the live schema, so widening it needs no migration and this API refuses no value. */
            end_reason: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        /**
         * @description Field-for-field `RemoteDriveReassignment` / `DriveReassignmentUpload`.
         *
         *     A correction RULE over `drives`, not a mutation of the drives themselves:
         *     "samples in this window belong to `new_vehicle_id`, not `vehicle_id`". The
         *     window is a time RANGE rather than a list of row ids, so that samples
         *     landing in it later, from any device, are still caught.
         *
         *     Nothing here refuses a rule that names the same vehicle twice. That is the
         *     live schema's own choice and it is deliberate: such a rule is a no-op the
         *     app should never produce, and a client retry of an already-applied
         *     correction is a legitimate no-op write.
         */
        DriveReassignment: {
            /** Format: uuid */
            readonly id: string;
            sync_id: string;
            /** Format: uuid */
            vehicle_id: string;
            /** Format: uuid */
            new_vehicle_id: string;
            /** Format: date-time */
            from_at: string;
            /** Format: date-time */
            to_at: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        Event: {
            /** Format: uuid */
            readonly id: string;
            title: string;
            /** Format: date-time */
            starts_at?: string | null;
            /** Format: date-time */
            ends_at?: string | null;
            all_day?: boolean;
            location?: string | null;
            notes?: string | null;
            source?: string;
            google_event_id?: string | null;
            done?: boolean;
            /** Format: date-time */
            done_at?: string | null;
            sort_order?: number | null;
            trigger_place_label?: string | null;
            repeat_kind?: string | null;
            repeat_every?: number | null;
            repeat_days_of_week?: string | null;
            repeat_day?: number | null;
            repeat_month?: number | null;
            repeat_end_kind?: string | null;
            /** Format: date */
            repeat_end_date?: string | null;
            repeat_end_count?: number | null;
            exact?: boolean;
            exact_downgraded?: boolean;
            /** Format: date-time */
            missed_at?: string | null;
            /** Format: date-time */
            missed_dismissed_at?: string | null;
            /** Format: date-time */
            logged_at?: string | null;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid?: string | null;
            structured_meta?: unknown;
            kind?: string;
        };
        /**
         * @description Field-for-field `RemoteGroceryStaple` / `GroceryStapleFields`
         *     (`LastAspectsBackend.kt`).
         *
         *     `grocery_staples` carries no CHECK constraints at all - CONSTRAINTS.md's
         *     own no-constraints list names it - only `grocery_staples_name_unique` and
         *     the `origin_guid` unique. The two refusals below are therefore this API's
         *     own, and each has a reason beyond tidiness: a blank `name` would take the
         *     unique name slot the real staple wants, and a `times_bought` below 1 would
         *     contradict the column's own `default 1`, which exists because a staple
         *     only becomes one by being bought at least once.
         */
        GroceryStaple: {
            /** Format: uuid */
            readonly id: string;
            name: string;
            display_name: string;
            /** @default 1 */
            times_bought: number;
            /** Format: date-time */
            last_bought_at: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description The 200 body. `db` is the only key, and it is only ever `"ok"` -
         *     anything else took the 503 branch below.
         */
        Health: {
            /** @description Always "ok" on a 200. */
            db: string;
        };
        /**
         * @description The 503 body. `detail` is psycopg's own message, verbatim - the
         *     check says what it actually found rather than a generic failure.
         */
        HealthError: {
            /** @description Always "error" on a 503. */
            db: string;
            detail: string;
        };
        /**
         * @description The 200 body, and it covers BOTH no-write outcomes because they are one
         *     status code and a client branches on `outcome`, not on the shape.
         *
         *     `ALREADY_COMMITTED` carries `content_sha256` and `note`; `QUARANTINED`
         *     carries `reason`. `inserted` is 0 in both - nothing was written either way.
         *     A quarantine is a VERDICT, not a transport failure (see this module's own
         *     doc comment for why it is not a 4xx), so it must never be surfaced as
         *     "something went wrong" or retried blindly.
         */
        IngestOutcome: {
            /** @description `ALREADY_COMMITTED` or `QUARANTINED`. */
            outcome: string;
            /** @description Always 0 here. Nothing was written. */
            inserted: number;
            /** @description `ALREADY_COMMITTED` only. */
            content_sha256?: string;
            /** @description `ALREADY_COMMITTED` only. */
            note?: string;
            /** @description `QUARANTINED` only: what the gate found, in words. Stored on `ingested_files.quarantine_reason` too, so the file is not re-offered with no memory of why it failed. */
            reason?: string;
        };
        /**
         * @description * `NEW` - New
         *     * `INGESTED` - Ingested
         *     * `QUARANTINED` - Quarantined
         *     * `UNREADABLE` - Unreadable
         *     * `DUPLICATE_CONTENT` - Duplicate Content
         *     * `NEEDS_LLM` - Needs Llm
         * @enum {string}
         */
        IngestStateEnum: "NEW" | "INGESTED" | "QUARANTINED" | "UNREADABLE" | "DUPLICATE_CONTENT" | "NEEDS_LLM";
        /**
         * @description One `public.ingested_files` row.
         *
         *     `content_sha256` is the idempotency key the whole ingest path turns on: a
         *     second post of the same bytes is a no-op, which is what makes a lost
         *     acknowledgement retryable rather than ambiguous
         *     (`ingest.views._already_committed`'s own doc comment). `quarantine_reason`
         *     is the sentence the gate wrote when it refused, in the same words the
         *     caller was given at the time.
         */
        IngestedFile: {
            /** Format: uuid */
            readonly id: string;
            readonly content_sha256: string;
            readonly source_file_id: string | null;
            readonly display_name: string | null;
            /** Format: int64 */
            readonly size_bytes: number | null;
            /**
             * @description Where this document got to. INGESTED means the section 4 gate ran and committed it; QUARANTINED means the gate ran and REFUSED it, and `quarantine_reason` says why in words; UNREADABLE, DUPLICATE_CONTENT and NEEDS_LLM are the three ways it never reached the gate at all. A QUARANTINED file wrote no rows anywhere - that is the point of it.
             *
             *     * `NEW` - New
             *     * `INGESTED` - Ingested
             *     * `QUARANTINED` - Quarantined
             *     * `UNREADABLE` - Unreadable
             *     * `DUPLICATE_CONTENT` - Duplicate Content
             *     * `NEEDS_LLM` - Needs Llm
             */
            readonly state: components["schemas"]["IngestStateEnum"];
            /** @description The gate's own sentence, or null. Written to be shown to a person: it says which figures did not agree, not merely that something failed. */
            readonly quarantine_reason: string | null;
            /**
             * Format: date-time
             * @description The first time this content was posted. FIXED after the first insert - not a change watermark. Page this feed with `since` against `last_attempt_at`, which is what the `next` cursor carries.
             */
            readonly first_seen_at: string;
            /**
             * Format: date-time
             * @description The last time this content was posted, rewritten on every commit and every retry. The column this feed's `?since=` and `next` are keyed on.
             */
            readonly last_attempt_at: string;
        };
        /**
         * @description One `public.ledger_transactions` row.
         *
         *     `provenance` is the load-bearing field on the wire, not decoration.
         *     `UNRECONCILED` means section 4 rule 7: the source stated no anchor, the
         *     row is provisional, it is transient, and **every surface that renders one
         *     must say so in words** - which for this API means a client cannot render
         *     this row without reading this field. `statement_id` is null on exactly
         *     those rows and non-null on every other, by
         *     `ledger_txn_header_matches_provenance`, so the two always agree.
         */
        LedgerTransaction: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            readonly statement_id: string | null;
            readonly account_last4: string;
            readonly account_nickname: string;
            readonly currency: string;
            /** Format: date */
            readonly txn_date: string;
            readonly description: string;
            /** Format: int64 */
            readonly amount_cents: number;
            /** Format: int64 */
            readonly balance_cents: number | null;
            readonly line_ref: string;
            readonly category: string | null;
            readonly category_pending: boolean;
            /** Format: date-time */
            readonly pending_logged_at: string | null;
            /**
             * Format: uuid
             * @description The id of the row this one reverses, or null. A gated row is never edited; it is corrected by posting a reversal and a replacement.
             */
            readonly reversal_of: string | null;
            /**
             * @description DETERMINISTIC / LLM_RECONCILED / UNRECONCILED / USER. **UNRECONCILED is provisional** (CLAUDE.md section 4 rule 7): the source stated no anchor, the figure is unverified, and any surface showing it - or any total containing it - must say so in words, never by colour or a glyph alone.
             *
             *     * `DETERMINISTIC` - Deterministic
             *     * `LLM_RECONCILED` - Llm Reconciled
             *     * `UNRECONCILED` - Unreconciled
             *     * `USER` - User
             */
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            readonly origin_guid: string | null;
        };
        LoginRequest: {
            /** Format: email */
            email: string;
            password: string;
            device_name: string;
        };
        LoginResponse: {
            token: string;
            /** Format: uuid */
            user_id: string;
        };
        /**
         * @description Field-for-field `RemoteMaintenanceSchedule` / `MaintenanceScheduleUpload`.
         *
         *     Deliberately carries NO `last_done_mileage` or `last_done_date`, matching
         *     the engine's own decision: when a service was last done is a fact about
         *     SERVICE HISTORY, and duplicating it here is how the two drift into
         *     disagreeing. "Is this due" is computed by reading the latest matching
         *     `service_history` row against these intervals.
         */
        MaintenanceSchedule: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            vehicle_id: string;
            service_name: string;
            interval_miles?: number | null;
            interval_months?: number | null;
            /** @description Free text rather than a constrained set - the engine left this open, and values like 'SEEDED' and a manual entry both occur. */
            interval_source: string;
            /** @default false */
            never_done: boolean;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        MeResponse: {
            /** Format: uuid */
            user_id: string;
            /** Format: email */
            email: string;
            device_name: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        MealLog: {
            /** Format: uuid */
            readonly id: string;
            description: string;
            /** @description An estimate, not a measurement: the model's guess from the meal description. Never gated and never treated as a reconciled figure (CLAUDE.md section 4 rule 5). */
            calories_kcal?: number | null;
            /**
             * Format: double
             * @description An estimate, not a measurement: the model's guess from the meal description. Never gated and never treated as a reconciled figure (CLAUDE.md section 4 rule 5).
             */
            protein_g?: number | null;
            /**
             * Format: double
             * @description An estimate, not a measurement: the model's guess from the meal description. Never gated and never treated as a reconciled figure (CLAUDE.md section 4 rule 5).
             */
            carbs_g?: number | null;
            /**
             * Format: double
             * @description An estimate, not a measurement: the model's guess from the meal description. Never gated and never treated as a reconciled figure (CLAUDE.md section 4 rule 5).
             */
            fat_g?: number | null;
            /** Format: date-time */
            logged_at: string;
            source_image_path?: string | null;
            trust_tier: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        MealTarget: {
            /** Format: uuid */
            readonly id: string;
            calories_kcal: number;
            /** Format: double */
            protein_g: number;
            /** Format: double */
            carbs_g: number;
            /** Format: double */
            fat_g: number;
            /** Format: date */
            effective_from_date: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        Memory: {
            /** Format: uuid */
            readonly id: string;
            text: string;
            /** Format: date-time */
            logged_at: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        MemoryAudit: {
            /** Format: uuid */
            readonly id: string;
            event: string;
            store: string;
            detail: string;
            /** Format: int64 */
            ref_id?: number | null;
            vehicle_id?: string | null;
            /** Format: date-time */
            logged_at: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description What `POST .../batch/` answers with.
         *
         *     Four numbers rather than one, because a batch upload can partly no-op for
         *     two completely different reasons and a client that cannot tell them apart
         *     cannot tell a healthy retry from a bug. `FleetBackend.uploadObdSampleBatch`
         *     itself only reads success or failure - its own doc explains that decoding
         *     500 rows back to count them would spend the request-size budget the
         *     batching exists to protect - and these four cost one integer each.
         */
        ObdBatchResult: {
            /** @description How many samples were in the request body. */
            received: number;
            /** @description How many rows this call actually wrote. Zero on a full re-post. */
            inserted: number;
            /** @description How many were already stored under the same natural key `(vehicle_id, pid, recorded_at)` and were therefore skipped. A re-post of a batch that already landed reports every row here, which is what makes an unacknowledged upload safe to retry. */
            already_present: number;
            /** @description How many rows in THIS body shared a natural key with an earlier row in the same body and were folded into it. Normally 0. Non-zero means the client sent two readings claiming the same car, PID and instant - which the table cannot store separately and which are indistinguishable to it - so the first was kept. Reported rather than silently dropped. */
            duplicates_in_batch: number;
        };
        /**
         * @description One `public.obd_samples` row as the windowed feed returns it.
         *
         *     Not a `SyncedSerializer`: that base's `create`/`update` exist to write rows
         *     through the four generic routes, and this table has no such route. Reads
         *     only.
         *
         *     **No `id` on the wire, and that is deliberate rather than an oversight.**
         *     `RemoteObdSample`'s own doc comment: "no `serverId`/`syncId` at all - the
         *     table's own natural key is `(vehicle_id, pid, recorded_at)`, which
         *     `FleetSync`'s pull-side dedup matches against directly rather than needing a
         *     separate identity column". There is no detail route to address a sample by
         *     its uuid, so publishing one would hand clients an identity that addresses
         *     nothing and invite them to key a cache on it.
         */
        ObdSample: {
            /** Format: uuid */
            readonly vehicle_id: string;
            pid: string;
            /** Format: double */
            value: number;
            unit: string;
            /**
             * Format: date-time
             * @description When the reading was taken from the car. Part of the natural key `(vehicle_id, pid, recorded_at)`, and the column this feed's `?since=` and `next` cursor are keyed on.
             */
            recorded_at: string;
            /** Format: double */
            lat?: number | null;
            /** Format: double */
            lng?: number | null;
            /**
             * Format: date-time
             * @description When this server stored the sample - NOT when it was read. The two can be far apart: the phone batches uploads and resumes them across sessions.
             */
            created_at: string;
        };
        ObdSampleCount: {
            /** @description How many `obd_samples` rows exist, for the whole table or for one vehicle. */
            count: number;
        };
        /**
         * @description One sample in a `POST .../batch/` body.
         *
         *     A plain `Serializer`, not a `ModelSerializer`, because nothing here goes
         *     through the ORM: the insert is one statement with an explicit
         *     `on conflict (vehicle_id, pid, recorded_at) do nothing` (see
         *     `ObdSampleBatchView`), and a model serializer would suggest a per-row
         *     `.save()` that does not happen.
         *
         *     `vehicle_id` is a bare `UUIDField` rather than a `PrimaryKeyRelatedField`
         *     for a measured reason: the related field issues one SELECT per row to prove
         *     the vehicle exists, which for a 500-row batch is 500 round trips against a
         *     remote Postgres. The view checks each DISTINCT vehicle once instead, which
         *     is the same guarantee at 1/500th the cost.
         */
        ObdSampleUpload: {
            /** Format: uuid */
            vehicle_id: string;
            pid: string;
            /** Format: double */
            value: number;
            unit: string;
            /** Format: date-time */
            recorded_at: string;
            /** Format: double */
            lat?: number | null;
            /** Format: double */
            lng?: number | null;
        };
        /**
         * @description Field-for-field `RemoteOilAnalysis` / `OilAnalysisUpload`: a used-oil lab
         *     report, voice-entered or typed by the driver.
         *
         *     USER provenance, not DETERMINISTIC, and the distinction is the point: a
         *     person transcribed these numbers, code did not derive them. Every figure
         *     here is as trustworthy as the typing.
         */
        OilAnalysis: {
            /** Format: uuid */
            readonly id: string;
            sync_id: string;
            /** Format: uuid */
            vehicle_id: string;
            /** Format: date-time */
            analyzed_at: string;
            mileage?: number | null;
            /** @default  */
            oil_brand: string;
            /** @default  */
            oil_grade: string;
            drain_interval_miles?: number | null;
            /** @description Parts per million. Null means the lab did not report iron, never 0 - a report that omits an element is not the same fact as one that measured zero of it. The same is true of every metal and contaminant here. */
            iron?: number | null;
            copper?: number | null;
            lead?: number | null;
            tin?: number | null;
            aluminum?: number | null;
            chromium?: number | null;
            nickel?: number | null;
            sodium?: number | null;
            potassium?: number | null;
            silicon?: number | null;
            boron?: number | null;
            magnesium?: number | null;
            /** Format: double */
            fuel_percent?: number | null;
            /** Format: double */
            water_percent?: number | null;
            /** Format: double */
            tbn?: number | null;
            /** Format: double */
            viscosity_cst?: number | null;
            /** @default  */
            lab_notes: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        PagedBodyweightLog: {
            results: components["schemas"]["BodyweightLog"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedBudgetTarget: {
            results: components["schemas"]["BudgetTarget"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedBuildEntry: {
            results: components["schemas"]["BuildEntry"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedCategory: {
            results: components["schemas"]["Category"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedCategoryRule: {
            results: components["schemas"]["CategoryRule"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedChassisQuirk: {
            results: components["schemas"]["ChassisQuirk"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedChecklist: {
            results: components["schemas"]["Checklist"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedChecklistItem: {
            results: components["schemas"]["ChecklistItem"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedCodeClearEvent: {
            results: components["schemas"]["CodeClearEvent"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedCodeEvent: {
            results: components["schemas"]["CodeEvent"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedCompanionMemory: {
            results: components["schemas"]["CompanionMemory"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedDrive: {
            results: components["schemas"]["Drive"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedDriveReassignment: {
            results: components["schemas"]["DriveReassignment"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedEvent: {
            results: components["schemas"]["Event"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedGroceryStaple: {
            results: components["schemas"]["GroceryStaple"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedIngestedFile: {
            results: components["schemas"]["IngestedFile"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedLedgerTransaction: {
            results: components["schemas"]["LedgerTransaction"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedMaintenanceSchedule: {
            results: components["schemas"]["MaintenanceSchedule"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedMealLog: {
            results: components["schemas"]["MealLog"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedMealTarget: {
            results: components["schemas"]["MealTarget"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedMemory: {
            results: components["schemas"]["Memory"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedMemoryAudit: {
            results: components["schemas"]["MemoryAudit"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedObdSample: {
            results: components["schemas"]["ObdSample"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedOilAnalysis: {
            results: components["schemas"]["OilAnalysis"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedPlace: {
            results: components["schemas"]["Place"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedReceipt: {
            results: components["schemas"]["Receipt"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedReceiptLineItem: {
            results: components["schemas"]["ReceiptLineItem"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedServiceHistory: {
            results: components["schemas"]["ServiceHistory"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedSleepLog: {
            results: components["schemas"]["SleepLog"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedSleepTarget: {
            results: components["schemas"]["SleepTarget"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedStatement: {
            results: components["schemas"]["Statement"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedVehicle: {
            results: components["schemas"]["Vehicle"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedVehicleSpec: {
            results: components["schemas"]["VehicleSpec"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedVoiceNote: {
            results: components["schemas"]["VoiceNote"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedWorkoutPlan: {
            results: components["schemas"]["WorkoutPlan"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedWorkoutPlanItem: {
            results: components["schemas"]["WorkoutPlanItem"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PagedWorkoutSetLog: {
            results: components["schemas"]["WorkoutSetLog"][];
            /**
             * Format: date-time
             * @description Cursor for the next page: hand it back as `?since=`. Null means this was the last page.
             */
            next: string | null;
        };
        PatchedChecklist: {
            /** Format: uuid */
            readonly id?: string;
            name?: string;
            schedule_kind?: string | null;
            schedule_every?: number | null;
            schedule_days_of_week?: string | null;
            sort_order?: number;
            archived?: boolean;
            /** Format: date-time */
            readonly created_at?: string;
            /** Format: date-time */
            readonly updated_at?: string;
            /** Format: date-time */
            readonly deleted_at?: string | null;
            sync_id?: string | null;
        };
        PatchedChecklistItem: {
            /** Format: uuid */
            readonly id?: string;
            /** Format: uuid */
            checklist?: string;
            text?: string;
            sort_order?: number;
            /** Format: date-time */
            readonly created_at?: string;
            /** Format: date-time */
            readonly updated_at?: string;
            /** Format: date-time */
            readonly deleted_at?: string | null;
            sync_id?: string | null;
            measure_unit?: string | null;
            /** Format: double */
            measure_target?: number | null;
            measure_direction?: string | null;
        };
        PatchedEvent: {
            /** Format: uuid */
            readonly id?: string;
            title?: string;
            /** Format: date-time */
            starts_at?: string | null;
            /** Format: date-time */
            ends_at?: string | null;
            all_day?: boolean;
            location?: string | null;
            notes?: string | null;
            source?: string;
            google_event_id?: string | null;
            done?: boolean;
            /** Format: date-time */
            done_at?: string | null;
            sort_order?: number | null;
            trigger_place_label?: string | null;
            repeat_kind?: string | null;
            repeat_every?: number | null;
            repeat_days_of_week?: string | null;
            repeat_day?: number | null;
            repeat_month?: number | null;
            repeat_end_kind?: string | null;
            /** Format: date */
            repeat_end_date?: string | null;
            repeat_end_count?: number | null;
            exact?: boolean;
            exact_downgraded?: boolean;
            /** Format: date-time */
            missed_at?: string | null;
            /** Format: date-time */
            missed_dismissed_at?: string | null;
            /** Format: date-time */
            logged_at?: string | null;
            readonly provenance?: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at?: string;
            /** Format: date-time */
            readonly updated_at?: string;
            /** Format: date-time */
            readonly deleted_at?: string | null;
            origin_guid?: string | null;
            structured_meta?: unknown;
            kind?: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        Place: {
            /** Format: uuid */
            readonly id: string;
            label: string;
            /** Format: double */
            latitude: number;
            /** Format: double */
            longitude: number;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        /**
         * @description * `DETERMINISTIC` - Deterministic
         *     * `LLM_RECONCILED` - Llm Reconciled
         *     * `UNRECONCILED` - Unreconciled
         *     * `USER` - User
         * @enum {string}
         */
        ProvenanceEnum: "DETERMINISTIC" | "LLM_RECONCILED" | "UNRECONCILED" | "USER";
        /**
         * @description One `public.receipts` header, with the anchors the gate checked it
         *     against (section 4 rule 8) and the residual it could not explain (rule 7).
         *
         *     `ingested_file_id` is nullable here and NOT on `statements`: a receipt can
         *     reach the gate from a photo the phone took rather than from a file it
         *     scanned, so there is not always a file row to point at.
         */
        Receipt: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            readonly ingested_file_id: string | null;
            readonly store: string;
            /** Format: date */
            readonly purchase_date: string;
            readonly currency: string;
            /**
             * Format: int64
             * @description The total the receipt printed, in cents. The anchor the line items were reconciled against (CLAUDE.md section 4 rule 2).
             */
            readonly total_cents: number;
            /** Format: int64 */
            readonly subtotal_cents: number | null;
            /** Format: int64 */
            readonly tax_cents: number | null;
            /** Format: int64 */
            readonly other_charges_cents: number | null;
            readonly photo_object_path: string | null;
            /**
             * @description DETERMINISTIC / LLM_RECONCILED / UNRECONCILED / USER. UNRECONCILED is provisional and transient: it is superseded, and physically deleted, when a document that DID pass the gate covers the same purchase.
             *
             *     * `DETERMINISTIC` - Deterministic
             *     * `LLM_RECONCILED` - Llm Reconciled
             *     * `UNRECONCILED` - Unreconciled
             *     * `USER` - User
             */
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            readonly origin_guid: string | null;
            /**
             * Format: int64
             * @description Null on every healthy receipt. Non-null means the total is MORE than the captured lines explain and the difference could not be re-verified (CLAUDE.md section 4 rule 7's 2026-08-26 amendment). `provenance` reads UNRECONCILED on exactly those rows, by check constraint, so the two always agree. **Never add this into a total or any other anchor** - it is the residual the arithmetic could not explain, not a figure to reconcile against - and any surface showing such a receipt must say in words that it is unverified.
             */
            readonly unaccounted_cents: number | null;
        };
        ReceiptAnchors: {
            total_cents: number;
            subtotal_cents: number | null;
            tax_cents: number | null;
            other_charges_cents: number | null;
            note: string;
        };
        /** @description The 201 body: line items were written. */
        ReceiptCommitted: {
            /** @description Always `COMMITTED` on a 201. */
            outcome: string;
            /** Format: uuid */
            receipt_id: string;
            /** @description Line items written. */
            inserted: number;
            anchors: components["schemas"]["ReceiptAnchors"];
            estimates: components["schemas"]["ReceiptEstimates"];
        };
        /**
         * @description Section 4 rule 5 as a response a caller cannot miss: anything the
         *     document does not state is an estimate and is labelled one.
         */
        ReceiptEstimates: {
            fields: string[];
            items_carrying_an_estimate: number;
            note: string;
        };
        /**
         * @description One `public.receipt_line_items` row: what the receipt printed, plus four
         *     macro estimates it did not. See this module's doc comment on rule 5, and on
         *     why `quantity` and the estimates render as strings rather than numbers.
         */
        ReceiptLineItem: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            readonly receipt_id: string;
            readonly name: string;
            /** Format: decimal */
            readonly quantity: string;
            /** Format: int64 */
            readonly unit_price_cents: number | null;
            /**
             * Format: int64
             * @description In cents. Summed across a receipt's lines, this is what the gate checked against the printed total (CLAUDE.md section 4 rule 2).
             */
            readonly total_price_cents: number;
            /**
             * Format: decimal
             * @description An estimate, not a measurement: the model's guess from the product name. A receipt never prints this. Excluded from every reconciliation check and never a gated figure (CLAUDE.md section 4 rule 5); any surface showing it must say it is an estimate.
             */
            readonly estimated_calories_kcal: string | null;
            /**
             * Format: decimal
             * @description An estimate, not a measurement: the model's guess from the product name. A receipt never prints this. Excluded from every reconciliation check and never a gated figure (CLAUDE.md section 4 rule 5); any surface showing it must say it is an estimate.
             */
            readonly estimated_protein_g: string | null;
            /**
             * Format: decimal
             * @description An estimate, not a measurement: the model's guess from the product name. A receipt never prints this. Excluded from every reconciliation check and never a gated figure (CLAUDE.md section 4 rule 5); any surface showing it must say it is an estimate.
             */
            readonly estimated_carbs_g: string | null;
            /**
             * Format: decimal
             * @description An estimate, not a measurement: the model's guess from the product name. A receipt never prints this. Excluded from every reconciliation check and never a gated figure (CLAUDE.md section 4 rule 5); any surface showing it must say it is an estimate.
             */
            readonly estimated_fat_g: string | null;
            /** Format: uuid */
            readonly reversal_of: string | null;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            readonly origin_guid: string | null;
        };
        /**
         * @description Field-for-field `RemoteServiceHistory` / `ServiceHistoryUpload`.
         *
         *     `kind` is the fleet aspect's own small provenance and is deliberately a
         *     separate column from `provenance`: provenance says how the ROW was
         *     produced, `kind` says how the EVENT was witnessed. OBSERVED means LEGION
         *     saw it or read it from a document; ASSERTED means the driver said so with
         *     no independent evidence. A client rendering a service record must not
         *     collapse the two, for the same reason section 4 rule 5 will not let an
         *     estimate be shown as a measurement.
         */
        ServiceHistory: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            vehicle_id: string;
            service_name: string;
            mileage?: number | null;
            /** Format: date */
            service_date?: string | null;
            /**
             * Format: int64
             * @description In cents (CLAUDE.md section 4 rule 3), or null when no figure was recorded. Unlike `build_entries.cost_cents` this column carries no non-negative CHECK on the live schema, so none is invented here.
             */
            cost_cents?: number | null;
            /** @description OBSERVED means LEGION saw it happen or read it from a document; ASSERTED means the driver said so, with no independent evidence. NOT the same axis as `provenance`, which says how the row was produced rather than how the event was witnessed. */
            kind: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid?: string | null;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        SleepLog: {
            /** Format: uuid */
            readonly id: string;
            /** Format: date */
            sleep_date: string;
            duration_minutes: number;
            quality?: number | null;
            notes?: string | null;
            /** Format: date-time */
            logged_at: string;
            trust_tier: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        SleepTarget: {
            /** Format: uuid */
            readonly id: string;
            target_minutes: number;
            /** Format: date */
            effective_from_date: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description One `public.statements` row: a bank statement's header and, crucially,
         *     the three anchors the gate checked it against (section 4 rule 8).
         *
         *     `ingested_file_id` rather than DRF's default `ingested_file` because that
         *     is the column's real name, and the name every existing client already
         *     reads - these rows reached the phone through PostgREST, which renders the
         *     physical column. Renaming it here would have been a gratuitous break for
         *     a tidier-looking field.
         */
        Statement: {
            /** Format: uuid */
            readonly id: string;
            /** Format: uuid */
            readonly ingested_file_id: string;
            readonly account_last4: string;
            readonly account_nickname: string;
            readonly currency: string;
            /** Format: date */
            readonly period_start: string;
            /** Format: date */
            readonly period_end: string;
            /**
             * Format: int64
             * @description The total the statement itself printed, in cents. NULL means the document printed none, which `statements_total_only_null_if_deterministic` permits only for a deterministically parsed statement. Never synthesised from the sum of the lines - that would make the check an identity (CLAUDE.md section 4 rule 6).
             */
            readonly stated_total_cents: number | null;
            /**
             * Format: int64
             * @description An anchor the gate checked against, in cents (section 4 rule 8).
             */
            readonly opening_balance_cents: number;
            /**
             * Format: int64
             * @description An anchor the gate checked against, in cents (section 4 rule 8).
             */
            readonly closing_balance_cents: number;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
        };
        /**
         * @description CLAUDE.md section 4 rule 8, echoed. The AUTHORITY is the stored row on
         *     `public.statements`; this copy is so a caller just told COMMITTED can see the
         *     three figures that earned it without a second round trip.
         */
        StatementAnchors: {
            /** @description Null means the bank printed none. **Never derived from sum(lines)** - that would make the check an identity. */
            stated_total_cents: number | null;
            opening_balance_cents: number;
            closing_balance_cents: number;
            note: string;
        };
        /** @description The 201 body: rows were written. */
        StatementCommitted: {
            /** @description Always `COMMITTED` on a 201. */
            outcome: string;
            /** Format: uuid */
            statement_id: string;
            /** @description Ledger transactions written. */
            inserted: number;
            duplicates_skipped: number;
            restatements_skipped: number;
            /** @description Rule 7 provisional rows deleted because this verified file covers the same card and window. */
            provisional_superseded: number;
            anchors: components["schemas"]["StatementAnchors"];
        };
        /**
         * @description `POST /api/checklists/<id>/items/<item>/tick` body - `{day, value?,
         *     source?}`. Deliberately a plain `Serializer`, not a `ModelSerializer`
         *     over `ChecklistTick`: the measured-item refusal (`validate`) needs the
         *     ITEM's own `measure_unit`, which is not a field on this request body at
         *     all (the item is named in the URL, not the payload) - so the check is
         *     written by hand rather than leaning on a field-level `validate_<name>`
         *     that has no access to a sibling object.
         */
        TickRequest: {
            day: number;
            /** Format: double */
            value?: number | null;
            /** @default USER_REPORTED */
            source: string;
        };
        /**
         * @description Field-for-field `RemoteVehicle` / `VehicleUpload` (`FleetBackend.kt`).
         *
         *     `confirmed` and `archived` default to False when omitted, matching their own
         *     column defaults. **That is a statement about the whole row, not a merge**:
         *     PUT here means "the row should be in this state", so a flag left out of the
         *     body reads as false rather than as "leave whatever is there". The same is
         *     true of every optional field on every table in this API; it is called out
         *     on this one because `archived` is USER state a person set deliberately
         *     (ticket 27, 2026-08-29) and silently clearing it would be worse than
         *     silently clearing a spec sheet's blank string.
         */
        Vehicle: {
            /** Format: uuid */
            readonly id: string;
            name: string;
            make: string;
            model: string;
            year: number;
            trim?: string | null;
            engine?: string | null;
            /** @default false */
            confirmed: boolean;
            /** @description An odometer reading, paired with `odometer_baseline_at`. Both or neither: a baseline without its timestamp cannot be projected forward, so it is worse than none - it looks like knowledge and is not (`vehicles_odometer_baseline_paired`). */
            odometer_baseline?: number | null;
            /** Format: date-time */
            odometer_baseline_at?: string | null;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid?: string | null;
            /** @default false */
            archived: boolean;
            /** @description The MAC of the OBD dongle this car was last seen on. A best-effort rebuild HINT, never an identity: a car that changed dongles carries its old value here until the next sync, and nothing may be assigned on the strength of it alone. */
            last_obd_mac?: string | null;
        };
        /**
         * @description Field-for-field `RemoteVehicleSpec` / `VehicleSpecUpload`: the factory
         *     build details for one car, mostly NHTSA vPIC VIN-decode output.
         *
         *     DETERMINISTIC despite the three manually-typed columns (`paint_color`,
         *     `paint_code`, `build_notes`) that vPIC cannot supply - the column default
         *     says so and this matches it. `decoded_at` is null, not 0, for "never
         *     decoded": the phone stores 0L for that and carrying it forward would read
         *     as an actual moment in 1970.
         */
        VehicleSpec: {
            /** Format: uuid */
            vehicle_id: string;
            /** @default  */
            vin: string;
            engine_cylinders?: number | null;
            /** Format: double */
            displacement_l?: number | null;
            engine_hp?: number | null;
            /** @default  */
            engine_config: string;
            /** @default  */
            fuel_type: string;
            /** @default  */
            transmission_style: string;
            /** @default  */
            transmission_speeds: string;
            /** @default  */
            drive_type: string;
            /** @default  */
            body_class: string;
            doors?: number | null;
            /** @default  */
            series: string;
            /** @default  */
            vehicle_type: string;
            /** @default  */
            manufacturer: string;
            /** @default  */
            plant_city: string;
            /** @default  */
            plant_country: string;
            /** @default  */
            paint_color: string;
            /** @default  */
            paint_code: string;
            /** @default  */
            build_notes: string;
            /**
             * Format: date-time
             * @description Null means never decoded. Never 0 or an epoch timestamp - the phone stores 0L for this and it would read as an actual moment in 1970.
             */
            decoded_at?: string | null;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
        };
        /**
         * @description Field-for-field `RemoteVoiceNote`. See this module's doc comment for
         *     the audio column that is absent from both.
         */
        VoiceNote: {
            /** Format: uuid */
            readonly id: string;
            /** Format: date-time */
            started_at: string;
            /** Format: date-time */
            ended_at?: string | null;
            title?: string | null;
            summary?: string | null;
            transcript?: string | null;
            kind: string;
            readonly provenance: components["schemas"]["VoiceNoteProvenanceEnum"];
            /** @default false */
            interrupted: boolean;
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
        };
        /**
         * @description * `LLM_DERIVED` - LLM_DERIVED
         * @enum {string}
         */
        VoiceNoteProvenanceEnum: "LLM_DERIVED";
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        WorkoutPlan: {
            /** Format: uuid */
            readonly id: string;
            sessions_per_week: number;
            /** Format: date */
            effective_from_week: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description Base for every serializer this viewset drives.
         *
         *     Two behaviours, both ticket 04's own text:
         *
         *     1. **Unknown fields are refused, never dropped.** "A limb sending a
         *        column the server does not know is a version skew, and it says so
         *        with a 400 naming the field, never a silent drop." A phone one
         *        release ahead, writing a column this server has never heard of,
         *        must be told - a silent drop would look exactly like a successful
         *        write and lose the value.
         *     2. **The server facts are the server's.** `id`, `provenance`,
         *        `created_at`, `updated_at` and `deleted_at` are read-only on every
         *        subclass; `deleted_at` moves only via DELETE, `updated_at` only via
         *        the `touch_updated_at` trigger.
         *
         *     Subclasses set `Meta.model` / `Meta.fields` and, where the table's
         *     `provenance` column is not the shared `USER` default,
         *     `default_provenance`.
         */
        WorkoutPlanItem: {
            /** Format: uuid */
            readonly id: string;
            exercise: string;
            target_sets_per_week: number;
            /** Format: date */
            effective_from_week: string;
            reps_per_set?: number | null;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
        /**
         * @description No `source_list_item_id`. `WorkoutSetLog.sourceListItemId` names a
         *     phone-local Room row id from a table that has no server counterpart, so
         *     it would be a dangling reference here - the body migration's own comment
         *     on that table, and `RemoteWorkoutSetLog`'s, both say so.
         */
        WorkoutSetLog: {
            /** Format: uuid */
            readonly id: string;
            exercise: string;
            sets: number;
            reps?: number | null;
            /** Format: double */
            weight_value?: number | null;
            weight_unit?: string | null;
            /** Format: date-time */
            logged_at: string;
            trust_tier: string;
            readonly provenance: components["schemas"]["ProvenanceEnum"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at: string;
            /** Format: date-time */
            readonly deleted_at: string | null;
            origin_guid: string;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    api_auth_login_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoginRequest"];
                "application/x-www-form-urlencoded": components["schemas"]["LoginRequest"];
                "multipart/form-data": components["schemas"]["LoginRequest"];
            };
        };
        responses: {
            /** @description A new device token. `token` is the raw key and is shown ONCE - the server stores only its hash and cannot hand it back again. Send it as `Authorization: Token <key>` on every later call. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LoginResponse"];
                };
            };
            /** @description Wrong email or password, or the account is inactive. No token was issued. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
            /** @description Rate limited: 5 attempts a minute per IP. `detail` says when to try again. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_auth_logout_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description This token is revoked. Every other device keeps working. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The request carried no device token, so there was nothing to revoke. Nothing was changed. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_auth_me_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The token is live and its user is a household member. `device_name` is the name the token was issued under at login. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MeResponse"];
                };
            };
            /** @description No token, an unknown token, or a revoked one. Ask for a new one. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
            /** @description The token is live but its user is not a household member. A `User` row alone is not enough - see `manage.py add_household_member`. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_bodyweight_logs_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedBodyweightLog"];
                };
            };
        };
    };
    api_body_bodyweight_logs_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BodyweightLog"];
                "application/x-www-form-urlencoded": components["schemas"]["BodyweightLog"];
                "multipart/form-data": components["schemas"]["BodyweightLog"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BodyweightLog"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_bodyweight_logs_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_meal_logs_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedMealLog"];
                };
            };
        };
    };
    api_body_meal_logs_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MealLog"];
                "application/x-www-form-urlencoded": components["schemas"]["MealLog"];
                "multipart/form-data": components["schemas"]["MealLog"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MealLog"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_meal_logs_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_meal_targets_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedMealTarget"];
                };
            };
        };
    };
    api_body_meal_targets_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MealTarget"];
                "application/x-www-form-urlencoded": components["schemas"]["MealTarget"];
                "multipart/form-data": components["schemas"]["MealTarget"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MealTarget"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_meal_targets_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_sleep_logs_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedSleepLog"];
                };
            };
        };
    };
    api_body_sleep_logs_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SleepLog"];
                "application/x-www-form-urlencoded": components["schemas"]["SleepLog"];
                "multipart/form-data": components["schemas"]["SleepLog"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SleepLog"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_sleep_logs_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_sleep_targets_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedSleepTarget"];
                };
            };
        };
    };
    api_body_sleep_targets_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SleepTarget"];
                "application/x-www-form-urlencoded": components["schemas"]["SleepTarget"];
                "multipart/form-data": components["schemas"]["SleepTarget"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SleepTarget"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_sleep_targets_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_workout_plan_items_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedWorkoutPlanItem"];
                };
            };
        };
    };
    api_body_workout_plan_items_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WorkoutPlanItem"];
                "application/x-www-form-urlencoded": components["schemas"]["WorkoutPlanItem"];
                "multipart/form-data": components["schemas"]["WorkoutPlanItem"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WorkoutPlanItem"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_workout_plan_items_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_workout_plans_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedWorkoutPlan"];
                };
            };
        };
    };
    api_body_workout_plans_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WorkoutPlan"];
                "application/x-www-form-urlencoded": components["schemas"]["WorkoutPlan"];
                "multipart/form-data": components["schemas"]["WorkoutPlan"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WorkoutPlan"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_workout_plans_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_body_workout_set_logs_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedWorkoutSetLog"];
                };
            };
        };
    };
    api_body_workout_set_logs_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WorkoutSetLog"];
                "application/x-www-form-urlencoded": components["schemas"]["WorkoutSetLog"];
                "multipart/form-data": components["schemas"]["WorkoutSetLog"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WorkoutSetLog"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_body_workout_set_logs_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_changes_retrieve: {
        parameters: {
            query?: {
                /** @description Comma-separated aspect names. Selects which top-level keys get populated: `checklists` fills `checklists`, `checklist_items` AND `checklist_ticks`; `body` fills all eight of its tables; `memory` all three. **Omitted or blank means every known aspect.** An unknown name is a 400 naming it - never a silently smaller response. **`fleet` fills eleven of its twelve tables and never `obd_samples`**: this endpoint is not paged and that table held 20,796 rows on 2026-09-07, so telemetry has its own paged, per-vehicle route at GET /api/fleet/obd_samples/?vehicle=<uuid>&since= instead. Its absence here is a design, not an empty table. */
                aspects?: ("events" | "checklists" | "body" | "fleet" | "ingest" | "ledger" | "memory" | "pantry" | "places" | "voice_notes")[];
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One key per TABLE, named for the table, each holding every row changed at or after `since` with tombstones included, oldest first. **Not paged**: a large first pull should use the per-table `?since=` routes, which are. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Changes"];
                };
            };
            /** @description An aspect name this server does not know. `detail` names it and lists the ones that exist. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Checklists changed at or after `since`, tombstones included, oldest first, 500 to a page. Items and ticks are their own routes; GET /api/changes returns all three together. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedChecklist"];
                };
            };
        };
    };
    api_checklists_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Checklist"];
                "application/x-www-form-urlencoded": components["schemas"]["Checklist"];
                "multipart/form-data": components["schemas"]["Checklist"];
            };
        };
        responses: {
            /** @description **Not created - this `sync_id` already exists**, and the body is the row that was already there, unchanged. A retried create is a no-op; a client tells the two apart by the status code, never by the body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Checklist"];
                };
            };
            /** @description Created. The body is the row as stored. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Checklist"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_checklists_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Checklist"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted, and NOT cascaded to items or ticks - a checklist's history is never rewritten by deleting the checklist. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_partial_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["PatchedChecklist"];
                "application/x-www-form-urlencoded": components["schemas"]["PatchedChecklist"];
                "multipart/form-data": components["schemas"]["PatchedChecklist"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Checklist"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_items_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path: {
                checklist_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Items on this checklist changed at or after `since`, tombstones included, oldest first, 500 to a page. An unknown `checklist_id` is an empty page here, not a 404 - this route does not read the parent. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedChecklistItem"];
                };
            };
        };
    };
    api_checklists_items_create: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ChecklistItem"];
                "application/x-www-form-urlencoded": components["schemas"]["ChecklistItem"];
                "multipart/form-data": components["schemas"]["ChecklistItem"];
            };
        };
        responses: {
            /** @description **Not created - this `sync_id` already exists**, and the body is the row that was already there, unchanged. A retried create is a no-op; a client tells the two apart by the status code, never by the body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChecklistItem"];
                };
            };
            /** @description Created. The URL's `checklist_id` wins over any `checklist` in the body. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChecklistItem"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_items_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
                item_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChecklistItem"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_items_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
                item_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted, and NOT cascaded to its ticks - dropping an item must not rewrite the history of days it was already ticked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_items_partial_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
                item_id: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["PatchedChecklistItem"];
                "application/x-www-form-urlencoded": components["schemas"]["PatchedChecklistItem"];
                "multipart/form-data": components["schemas"]["PatchedChecklistItem"];
            };
        };
        responses: {
            /** @description The row as stored. A `checklist` in the body is DROPPED, not honoured: an item cannot be reparented, and the URL is the only authority on which checklist it belongs to. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChecklistItem"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_items_tick_create: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
                item_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TickRequest"];
                "application/x-www-form-urlencoded": components["schemas"]["TickRequest"];
                "multipart/form-data": components["schemas"]["TickRequest"];
            };
        };
        responses: {
            /** @description A tick already existed for that day. Either an idempotent no-op (the FIRST tap's `ticked_at`/`value`/`source` are kept) or, if it had been unticked, the same row revived with a fresh `ticked_at`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChecklistTick"];
                };
            };
            /** @description A first tick for that day. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChecklistTick"];
                };
            };
            /** @description Nothing was recorded. Chiefly the measured-item refusal: an item with a `measure_unit` needs a `value`, and the message is the same sentence the phone's own controller uses. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_checklists_items_tick_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                checklist_id: string;
                day: number;
                item_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Idempotent: no tick on that day is still a 204, not a 404 - 'already untouched' and 'just unticked' read the same to a caller that does not care which happened. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_events_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Rows changed at or after `since`, tombstones included, oldest first, 500 to a page. This route does NOT accept `?active=1` - the synced routes do; here a client filters `deleted_at` itself. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedEvent"];
                };
            };
        };
    };
    api_events_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Event"];
                "application/x-www-form-urlencoded": components["schemas"]["Event"];
                "multipart/form-data": components["schemas"]["Event"];
            };
        };
        responses: {
            /** @description **Not created - this `origin_guid` already exists**, and the body is the row that was already there, unchanged. A retried create is a no-op, which is what makes a lost acknowledgement safe to retry. A client tells the two apart by the status code, never by the body. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Event"];
                };
            };
            /** @description Created. The body is the row as stored, server-minted `id`, `provenance` and timestamps included. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Event"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_events_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_events_partial_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["PatchedEvent"];
                "application/x-www-form-urlencoded": components["schemas"]["PatchedEvent"];
                "multipart/form-data": components["schemas"]["PatchedEvent"];
            };
        };
        responses: {
            /** @description The row as stored. Every field is optional on the way in; only the ones sent are changed. Sending `done` without `done_at` derives `done_at` from the database clock (or clears it when `done` is false). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Event"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_build_entries_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedBuildEntry"];
                };
            };
        };
    };
    api_fleet_build_entries_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BuildEntry"];
                "application/x-www-form-urlencoded": components["schemas"]["BuildEntry"];
                "multipart/form-data": components["schemas"]["BuildEntry"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BuildEntry"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_build_entries_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_chassis_quirks_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedChassisQuirk"];
                };
            };
        };
    };
    api_fleet_chassis_quirks_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `quirk_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ChassisQuirk"];
                "application/x-www-form-urlencoded": components["schemas"]["ChassisQuirk"];
                "multipart/form-data": components["schemas"]["ChassisQuirk"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ChassisQuirk"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_code_clear_events_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedCodeClearEvent"];
                };
            };
        };
    };
    api_fleet_code_clear_events_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CodeClearEvent"];
                "application/x-www-form-urlencoded": components["schemas"]["CodeClearEvent"];
                "multipart/form-data": components["schemas"]["CodeClearEvent"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CodeClearEvent"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_code_clear_events_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_code_events_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedCodeEvent"];
                };
            };
        };
    };
    api_fleet_code_events_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CodeEvent"];
                "application/x-www-form-urlencoded": components["schemas"]["CodeEvent"];
                "multipart/form-data": components["schemas"]["CodeEvent"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CodeEvent"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_code_events_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_drive_reassignments_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedDriveReassignment"];
                };
            };
        };
    };
    api_fleet_drive_reassignments_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DriveReassignment"];
                "application/x-www-form-urlencoded": components["schemas"]["DriveReassignment"];
                "multipart/form-data": components["schemas"]["DriveReassignment"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DriveReassignment"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_drive_reassignments_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_drives_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedDrive"];
                };
            };
        };
    };
    api_fleet_drives_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Drive"];
                "application/x-www-form-urlencoded": components["schemas"]["Drive"];
                "multipart/form-data": components["schemas"]["Drive"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Drive"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_drives_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_maintenance_schedules_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedMaintenanceSchedule"];
                };
            };
        };
    };
    api_fleet_maintenance_schedules_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `service_name`. */
                service_name: string;
                /** @description The row's `vehicle_id`. */
                vehicle_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MaintenanceSchedule"];
                "application/x-www-form-urlencoded": components["schemas"]["MaintenanceSchedule"];
                "multipart/form-data": components["schemas"]["MaintenanceSchedule"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MaintenanceSchedule"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_maintenance_schedules_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `service_name`. */
                service_name: string;
                /** @description The row's `vehicle_id`. */
                vehicle_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_obd_samples_list: {
        parameters: {
            query: {
                /** @description ISO-8601 UTC watermark, matched against `recorded_at` - the instant the sample was READ FROM THE CAR, not the instant it reached this server. That is the column `FleetBackend.fetchObdSamplesSince` filters on, and the only one that means anything to a client rebuilding a window of telemetry. Omitted means this vehicle's whole history, paged; the phone passes roughly the last 30 days (Kevin's 2026-09-03 OBD-volume ruling), and that window is the CLIENT's choice, never a narrowing this server applies on its behalf. */
                since?: string;
                /** @description REQUIRED. The `vehicles.id` whose samples to return. This is the one list route in the API with a mandatory filter: `obd_samples` held 20,796 rows on 2026-09-07 and an unbounded feed over it is a download, not a feed. Absent is a 400 naming this parameter; a uuid naming no vehicle is a 404, never an empty page - an empty page would read as 'this car has no telemetry' when the truth is 'there is no such car'. */
                vehicle: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of one vehicle's samples, oldest first. No tombstones, because this table has no tombstone column - telemetry is append-only and is corrected by a newer observation, never by editing an old one. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedObdSample"];
                };
            };
            /** @description `vehicle` was missing or was not a uuid. Nothing was returned. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
            /** @description `vehicle` named no vehicle this server knows. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_obd_samples_batch_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ObdSampleUpload"][];
                "application/x-www-form-urlencoded": components["schemas"]["ObdSampleUpload"][];
                "multipart/form-data": components["schemas"]["ObdSampleUpload"][];
            };
        };
        responses: {
            /** @description Nothing was written, and that is a normal answer: every sample in the batch was already stored under the same natural key, or the batch was empty. Not an error, and not something to retry differently. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ObdBatchResult"];
                };
            };
            /** @description At least one sample was written. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ObdBatchResult"];
                };
            };
            /** @description Nothing was written. The body was not a JSON array, a sample was malformed, the batch was over the size cap, or a `vehicle_id` named no vehicle. Field-level errors come back keyed by index. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_obd_samples_count: {
        parameters: {
            query?: {
                /** @description Optional. Narrows the count to one vehicle. Omitted counts the whole table, which is what `FleetBackend.countObdSamples` asks for. */
                vehicle?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ObdSampleCount"];
                };
            };
            /** @description `vehicle` was not a uuid. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
            /** @description `vehicle` named no vehicle this server knows. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_oil_analyses_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedOilAnalysis"];
                };
            };
        };
    };
    api_fleet_oil_analyses_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["OilAnalysis"];
                "application/x-www-form-urlencoded": components["schemas"]["OilAnalysis"];
                "multipart/form-data": components["schemas"]["OilAnalysis"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OilAnalysis"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_oil_analyses_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `sync_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_service_history_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedServiceHistory"];
                };
            };
        };
    };
    api_fleet_service_history_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ServiceHistory"];
                "application/x-www-form-urlencoded": components["schemas"]["ServiceHistory"];
                "multipart/form-data": components["schemas"]["ServiceHistory"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceHistory"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_service_history_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_fleet_vehicle_specs_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedVehicleSpec"];
                };
            };
        };
    };
    api_fleet_vehicle_specs_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `vehicle_id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VehicleSpec"];
                "application/x-www-form-urlencoded": components["schemas"]["VehicleSpec"];
                "multipart/form-data": components["schemas"]["VehicleSpec"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VehicleSpec"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_vehicles_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedVehicle"];
                };
            };
        };
    };
    api_fleet_vehicles_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Vehicle"];
                "application/x-www-form-urlencoded": components["schemas"]["Vehicle"];
                "multipart/form-data": components["schemas"]["Vehicle"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Vehicle"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_fleet_vehicles_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ingest_files_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedIngestedFile"];
                };
            };
        };
    };
    api_ingest_files_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IngestedFile"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ingest_receipt_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
                "application/x-www-form-urlencoded": {
                    [key: string]: unknown;
                };
                "multipart/form-data": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description Nothing was written, and that is a normal answer. Either this exact receipt was already committed, or the gate refused it and the refusal is now on record with its reason. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IngestOutcome"];
                };
            };
            /** @description The gate passed and line items were written. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReceiptCommitted"];
                };
            };
            /** @description The gate could not RUN - a missing `content_sha256`, a money field that is not an integer number of cents, a bad date, an `UNRECONCILED` provenance (there is no provisional path here), or a database constraint the payload tripped. Nothing was written, and no quarantine row was recorded either, because this is not a verdict about a document: it is a payload nothing could evaluate. Distinct from a quarantine, which is a 200. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ingest_statement_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
                "application/x-www-form-urlencoded": {
                    [key: string]: unknown;
                };
                "multipart/form-data": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description Nothing was written, and that is a normal answer. Either this exact file was already committed, or the gate refused it and the refusal is now on record with its reason. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IngestOutcome"];
                };
            };
            /** @description The gate passed and rows were written. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["StatementCommitted"];
                };
            };
            /** @description The gate could not RUN - a missing `content_sha256`, a money field that is not an integer number of cents, a bad date, an `UNRECONCILED` provenance (there is no provisional path here), or a database constraint the payload tripped. Nothing was written, and no quarantine row was recorded either, because this is not a verdict about a document: it is a payload nothing could evaluate. Distinct from a quarantine, which is a 200. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ledger_budget_targets_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedBudgetTarget"];
                };
            };
        };
    };
    api_ledger_budget_targets_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BudgetTarget"];
                "application/x-www-form-urlencoded": components["schemas"]["BudgetTarget"];
                "multipart/form-data": components["schemas"]["BudgetTarget"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BudgetTarget"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_ledger_budget_targets_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ledger_categories_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedCategory"];
                };
            };
        };
    };
    api_ledger_categories_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Category"];
                "application/x-www-form-urlencoded": components["schemas"]["Category"];
                "multipart/form-data": components["schemas"]["Category"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Category"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_ledger_categories_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ledger_category_rules_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedCategoryRule"];
                };
            };
        };
    };
    api_ledger_category_rules_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CategoryRule"];
                "application/x-www-form-urlencoded": components["schemas"]["CategoryRule"];
                "multipart/form-data": components["schemas"]["CategoryRule"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CategoryRule"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_ledger_category_rules_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ledger_statements_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedStatement"];
                };
            };
        };
    };
    api_ledger_statements_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Statement"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_ledger_transactions_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedLedgerTransaction"];
                };
            };
        };
    };
    api_ledger_transactions_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LedgerTransaction"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_memory_companion_memories_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
                /** @description The vehicle this device is currently in (the phone's `ActiveVehicle.current()` - a dongle MAC or a `car:<uuid>`). Supplying it applies the recall rule: every `driver` and `relationship` memory comes back, and `car_anchored` memories come back ONLY for this vehicle. **Omitting it returns everything**, because this route is also the replication feed and a replica has to be whole - see `api/memory.py`'s own module doc. Send it whenever the rows are going into a prompt; omit it when they are going into a local cache. */
                vehicle?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedCompanionMemory"];
                };
            };
        };
    };
    api_memory_companion_memories_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CompanionMemory"];
                "application/x-www-form-urlencoded": components["schemas"]["CompanionMemory"];
                "multipart/form-data": components["schemas"]["CompanionMemory"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanionMemory"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_memory_companion_memories_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_memory_memories_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedMemory"];
                };
            };
        };
    };
    api_memory_memories_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Memory"];
                "application/x-www-form-urlencoded": components["schemas"]["Memory"];
                "multipart/form-data": components["schemas"]["Memory"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Memory"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_memory_memories_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_memory_memory_audit_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedMemoryAudit"];
                };
            };
        };
    };
    api_memory_memory_audit_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MemoryAudit"];
                "application/x-www-form-urlencoded": components["schemas"]["MemoryAudit"];
                "multipart/form-data": components["schemas"]["MemoryAudit"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MemoryAudit"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_pantry_grocery_staples_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedGroceryStaple"];
                };
            };
        };
    };
    api_pantry_grocery_staples_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GroceryStaple"];
                "application/x-www-form-urlencoded": components["schemas"]["GroceryStaple"];
                "multipart/form-data": components["schemas"]["GroceryStaple"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GroceryStaple"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_pantry_grocery_staples_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `origin_guid`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_pantry_line_items_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedReceiptLineItem"];
                };
            };
        };
    };
    api_pantry_line_items_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReceiptLineItem"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_pantry_receipts_list: {
        parameters: {
            query?: {
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedReceipt"];
                };
            };
        };
    };
    api_pantry_receipts_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Receipt"];
                };
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_places_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedPlace"];
                };
            };
        };
    };
    api_places_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `label`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["Place"];
                "application/x-www-form-urlencoded": components["schemas"]["Place"];
                "multipart/form-data": components["schemas"]["Place"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Place"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_places_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `label`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_voice_notes_list: {
        parameters: {
            query?: {
                /** @description Narrows the feed to live rows (`deleted_at is null`). Composes with `since`. Anything else, `0` and `false` included, means the narrowing was not asked for and tombstones stay in - a caller who cannot be understood sees too much, never silently nothing. */
                active?: "1" | "on" | "true" | "yes";
                /** @description ISO-8601 UTC watermark. Returns rows with `updated_at >= since`, tombstones included, oldest first. **Omitted or unparsable means EVERYTHING, never nothing** (api/sync.parse_since), so a fresh client's first pull is the whole table. Hand back the `next` cursor from the previous page, or `server_time` from GET /api/changes; both are already `Z`-suffixed so they need no extra encoding. */
                since?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PagedVoiceNote"];
                };
            };
        };
    };
    api_voice_notes_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VoiceNote"];
                "application/x-www-form-urlencoded": components["schemas"]["VoiceNote"];
                "multipart/form-data": components["schemas"]["VoiceNote"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VoiceNote"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    api_voice_notes_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VoiceNote"];
                "application/x-www-form-urlencoded": components["schemas"]["VoiceNote"];
                "multipart/form-data": components["schemas"]["VoiceNote"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VoiceNote"];
                };
            };
            /** @description The write was refused and NOTHING was written. Two body shapes occur and both are JSON objects: `{"detail": "..."}` for an unknown field, a database refusal or a hand-written check, and `{"<field>": ["..."]}` for a field-level validation error. Both carry text meant to be shown to a person. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description This identity is the server's own primary key, and it has not issued this one. Nothing was created. POST the collection instead. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    api_voice_notes_destroy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The row's `id`. */
                identity: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Done. Soft-deleted (the tombstone row stays so an unsynced client learns of it). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such row. Nothing was changed. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Detail"];
                };
            };
        };
    };
    health_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The database answered `SELECT 1`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Health"];
                };
            };
            /** @description The database did not answer. The body carries psycopg's own message. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthError"];
                };
            };
        };
    };
    healthz_retrieve: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The database answered `SELECT 1`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Health"];
                };
            };
            /** @description The database did not answer. The body carries psycopg's own message. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthError"];
                };
            };
        };
    };
}
